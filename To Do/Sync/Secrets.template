// Secrets.template.swift — committed to git (safe, no real values)
// Copy this file to Secrets.swift and fill in your real values.
enum Secrets {
    static let supabaseURL = "https://your-project.supabase.co"
    static let supabaseKey = "your-supabase-service-role-key"
}
